export enum Language {
    ES = 'es-ES',
    EN = 'en-US',
    FR = 'fr-FR',
    JP = 'ja-JP',
}

export enum IdentityNumberType {
    NIC = 'nic',
}

export enum Status {
    Active = 'active',
    Inactive = 'inactive',
}

export enum WorkStatus {
    Offline = 'offline',
    Online = 'online',
    Paused = 'paused',
}

export enum Origin {
    API = 'api',
}

export enum WorkEntryType {
    Work = 'work',
}
